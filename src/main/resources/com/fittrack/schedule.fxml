<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.fittrack.controller.ScheduleController"
      spacing="16"
      style="-fx-padding: 24; -fx-background-color: #0F0F0F;">

    <Label text="Schedule" styleClass="screen-title" />

    <Label fx:id="nextReminderLabel"
           styleClass="card-subtitle"
           style="-fx-text-fill: #00E5FF; -fx-font-size: 14;" />

    <VBox styleClass="card" spacing="8" VBox.vgrow="ALWAYS">
        <Label text="PRIORITY REMINDERS" styleClass="card-title-accent" />

        <TableView fx:id="reminderTable"
                   styleClass="dark-table"
                   VBox.vgrow="ALWAYS">
            <columns>
                <TableColumn fx:id="labelColumn" text="Title" minWidth="200" />
                <TableColumn fx:id="timeColumn" text="Message" minWidth="280" />
                <TableColumn fx:id="noteColumn" text="Type" minWidth="180" />
            </columns>
            <placeholder>
                <Label text="No active reminders yet."
                       style="-fx-text-fill: #666;" />
            </placeholder>
        </TableView>

        <HBox alignment="CENTER_RIGHT">
            <Button text="Remove Selected"
                    styleClass="danger-button"
                    onAction="#removeSelectedReminder" />
        </HBox>
    </VBox>

    <VBox styleClass="card" spacing="10">
        <Label text="ADD MANUAL REMINDER" styleClass="card-title-accent" />

        <Label text="Body part inactivity reminders are automatic after 5 days without a logged session for that body part."
               styleClass="card-subtitle"
               wrapText="true" />

        <FlowPane hgap="12" vgap="12" prefWrapLength="1020">
            <VBox spacing="4" minWidth="220" prefWidth="260">
                <Label text="LABEL" styleClass="field-label" />
                <TextField fx:id="reminderLabelField"
                           promptText="e.g. Chest Day, Leg Day..."
                           styleClass="dark-input" />
            </VBox>

            <VBox spacing="4" minWidth="220" prefWidth="260">
                <Label text="DATE &amp; TIME" styleClass="field-label" />
                <TextField fx:id="reminderTimeField"
                           promptText="yyyy-MM-dd HH:mm"
                           styleClass="dark-input" />
            </VBox>

            <VBox spacing="4" minWidth="220" prefWidth="260">
                <Label text="NOTE (OPTIONAL)" styleClass="field-label" />
                <TextField fx:id="reminderNoteField"
                           promptText="Add a short note..."
                           styleClass="dark-input" />
            </VBox>

            <Button text="+ Add"
                    styleClass="primary-button"
                    onAction="#addReminder"
                    style="-fx-padding: 8 20;" />
        </FlowPane>
    </VBox>

</VBox>
